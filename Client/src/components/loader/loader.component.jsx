import React from 'react';

import { LoaderContainer, LoaderOverlay } from './loader.styles';

const Loader = () => (
    <LoaderOverlay>
        <LoaderContainer />
    </LoaderOverlay>
);

export default Loader;
